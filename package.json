{
  "private": true,
  "author": "Hydrate <dev@hydrate.io> (https://hydrate.io)",
  "license": "MIT",
  "repository": "hydrateio/react-live-props",
  "engines": {
    "node": ">=8",
    "npm": ">=5"
  },
  "scripts": {
    "test": "cd packages/react-live-props && yarn test",
    "test:watch": "cd packages/react-live-props && yarn test:watch",
    "build": "cd packages/react-live-props && yarn build",
    "prepare": "yarn build",
    "predeploy": "yarn build && cd packages/example && yarn install && yarn build",
    "deploy": "gh-pages -d packages/example/.docz/dist"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
